import { install } from '../../../utils'
import _PollutionSourceMonitor from './index.vue'

export const DvAimsPollutionSourceMonitor = install(_PollutionSourceMonitor)
export default _PollutionSourceMonitor
